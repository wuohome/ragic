.env
.env.local
__pycache__/
*.pyc
dist/
reports/
.DS_Store

screenshot-tmp/
*.png
perf-sa-key.json

# 🚨 P1 SECURITY: never commit Ragic admin key / backup html to public repo
# Origin: 2026-05-13 audit — Mac side `js/ragic-key.js` untracked but unprotected,
# 一次 `git add .` 就會洩 plaintext base64 admin key 上 github.com/wuohome/ragic
js/ragic-key.js
*.bak
*.bak-*
_verify/

# 🚨 2026-09-17: wrangler 本機狀態與設定不進 public repo
# `.wrangler/cache/wrangler-account.json` 帶 Cloudflare 帳號身分；
# wrangler.jsonc 是 Worker 設定，本 repo 是前端頁面 repo，不該收；
# backups/ 是本機暫存的舊版 html/patch。三者原本都會被 `git add .` 掃進去。
.wrangler/
wrangler.jsonc
backups/
